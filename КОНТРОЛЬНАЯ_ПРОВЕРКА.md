# Контрольная проверка системы диагностики компетенций

## ✅ Выполнено: Выравнивание статусов

### 1. База данных (всегда английские значения)
- ✅ `'pending'` - ожидает утверждения
- ✅ `'approved'` - согласовано руководителем
- ✅ `'completed'` - выполнено
- ✅ `'in_progress'` - в процессе
- ✅ `'draft'` - черновик

### 2. UI (локализованные значения)
- ✅ `'Ожидает'` / `'Ожидает утверждения'`
- ✅ `'Согласовано'`
- ✅ `'Выполнено'`
- ✅ `'В процессе'`
- ✅ `'Черновик'`

### 3. Централизованный маппер
✅ Создан `src/lib/statusMapper.ts` с функциями:
- `DB_STATUS` - константы для БД
- `STATUS_LABELS` - словарь для UI
- `getStatusLabel()` - получение UI-метки
- `isCompleted()` - проверка завершения
- `isPending()` - проверка ожидания

---

## ✅ Исправлены статусы в компонентах

### Компоненты UI
1. ✅ `AssessmentCompletionProgress.tsx` - использует `isCompleted()`
2. ✅ `RespondentStatusTable.tsx` - использует `isCompleted()`
3. ✅ `TeamMembersTable.tsx` - использует `'completed'`
4. ✅ `MyAssignmentsPage.tsx` - использует `'completed'`
5. ✅ `SkillSurveyPage.tsx` - использует `'completed'`
6. ✅ `Survey360Page.tsx` - использует `'completed'`, `'pending'`, `'approved'`
7. ✅ `UnifiedAssessmentPage.tsx` - устанавливает `'completed'`
8. ✅ `ColleagueSelectionDialog.tsx` - создает `'pending'`, маппинг статусов

### Хуки и утилиты
1. ✅ `useSurvey360Assignments.ts` - создает `'pending'` (был `'pending_approval'`)
2. ✅ `useTasks.ts` - работает с `'pending'`, `'completed'`

### Триггеры БД
1. ✅ `update_task_status_on_assignment_change` - проверяет `'completed'`
2. ✅ `update_assignment_on_survey_completion` - устанавливает `'completed'`

---

## ✅ Устранены критические ошибки

### 1. Статус `'pending_approval'` заменен на `'pending'`
**Где было:**
- `useSurvey360Assignments.ts` - создание assignments
- `Survey360Page.tsx` - фильтрация коллег
- `ColleagueSelectionDialog.tsx` - маппинг статусов

**Исправлено:**
- Везде используется `'pending'`

### 2. Статус `'выполнено'` заменен на `'completed'`
**Где было:**
- Множество компонентов проверяли кириллический статус
- Триггеры БД использовали `'выполнено'`

**Исправлено:**
- БД: всегда `'completed'`
- UI: отображает `'Выполнено'` через маппинг

### 3. Мигрированы существующие данные
```sql
UPDATE survey_360_assignments SET status = 'completed' WHERE status = 'выполнено';
UPDATE tasks SET status = 'completed' WHERE status = 'выполнено';
```

---

## 📋 Проверка флоу по ролям

### Роль: Участник диагностики
1. ✅ Видит задание "Комплексная диагностика"
2. ✅ Заходит в Survey360Page
3. ✅ Выбирает коллег → создаются assignments со статусом `'pending'`
4. ✅ Видит сообщение "Ожидается утверждение руководителем"
5. ✅ После утверждения статус → `'approved'`
6. ✅ Может начать самооценку
7. ✅ После завершения статус → `'completed'`

### Роль: Руководитель
1. ✅ Видит задание "Оценка подчиненного: ФИО"
2. ✅ В TeamMembersTable видит статус "Ожидает" для pending
3. ✅ Кликает "Утвердить список"
4. ✅ ManagerRespondentApproval загружает `status = 'pending'` и `assignment_type = 'peer'`
5. ✅ Утверждает → статус → `'approved'`
6. ✅ Создаются задачи для коллег с `status = 'pending'`
7. ✅ Может выполнить свою оценку → `'completed'`

### Роль: Коллега
1. ✅ Получает задачу после утверждения руководителем
2. ✅ Видит задачу с `status = 'pending'`
3. ✅ Проходит оценку
4. ✅ После завершения → `'completed'`

### Роль: HR/Админ
1. ✅ Создает диагностический этап
2. ✅ Назначает участников
3. ✅ Видит прогресс в TeamMembersTable
4. ✅ Все статусы отображаются корректно

---

## 🔍 Дополнительные проверки

### Проверка консистентности
- ✅ Нет смешивания `'pending'` и `'pending_approval'`
- ✅ Нет смешивания `'completed'` и `'выполнено'`
- ✅ Все фильтры используют правильные статусы
- ✅ Все триггеры обновлены

### Проверка UI
- ✅ Отображение статусов локализовано
- ✅ Бейджи используют правильные цвета
- ✅ Сообщения пользователям корректны

### Проверка БД
- ✅ Триггеры используют `'completed'`
- ✅ Существующие данные мигрированы
- ✅ RLS политики не нарушены

---

## ✅ ИТОГ: Система полностью выровнена

**Все статусы приведены к единому стандарту:**
- **БД:** английские значения (`'pending'`, `'approved'`, `'completed'`)
- **UI:** локализованные метки (`'Ожидает'`, `'Согласовано'`, `'Выполнено'`)
- **Маппинг:** централизованный через `statusMapper.ts`

**Критические ошибки устранены:**
- ❌ `'pending_approval'` → ✅ `'pending'`
- ❌ `'выполнено'` → ✅ `'completed'` (БД) + `'Выполнено'` (UI)

**Флоу работает корректно для всех ролей:**
- ✅ Участник
- ✅ Руководитель  
- ✅ Коллега
- ✅ HR/Админ
