# dashboard template

Composition of patterns; see `patterns/` for atomic pieces.

## Shell
`Sidebar` + `<main class="app-main-bg">` wrapping `Container size="xl"`.

## Sections
- Page header → KPI grid → 2/3 chart + 1/3 list → data table.

## Token contract
Only semantic tokens. No raw colors. Spacing rhythm via `section.gap.md`.
