# Search interface
- `Command` palette (cmdk) OR top-bar `Input` with leading search icon
- Results in `Popover` or routed page; rows use `interactive.bg.hover`
- Empty/loading states via `Skeleton` then `EmptyState`
