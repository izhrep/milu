# Dashboard layout
- App shell: `Sidebar` + `<main class="app-main-bg">`
- Page header: `Stack gap="sm"` with `Heading 2` + actions `Inline gap="sm"`
- KPI row: `Grid cols={4} gap="lg"` of `Card`
- Main grid: `Grid cols={3} gap="lg"`
- Charts use `--chart-1..5`.
