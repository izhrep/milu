# Auth form
- `Container size="sm"` → `Card` → `Stack gap="lg"`
- Heading level 2 + caption helper text
- `Form` with email + password `Input`s, `FormMessage` for errors
- Primary `Button` full-width
- Secondary link (`Button variant="link"`)
Tokens: card.bg, radius.card, shadow.card.
