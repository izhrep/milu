# Data table
- `Card` wrapper
- Toolbar: search `Input` + filter `Select`s + `Button` actions
- `Table` with sticky header, zebra rows via `background.secondary`
- Pagination footer
- Row hover uses `interactive.bg.hover`
