# Settings page
- Vertical `Tabs` on the left or top
- Each section: `Card` with `Heading 4` + `Form`
- Save bar sticky at bottom (only when dirty), using `Button variant="default"` + `ghost` cancel.
