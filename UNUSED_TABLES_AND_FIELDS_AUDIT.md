# Аудит неиспользуемых таблиц и полей в базе данных

**Дата аудита:** 13 ноября 2025  
**Статус:** Глубокий анализ завершен

---

## 📊 Сводная статистика

### Полностью неиспользуемые таблицы

| Таблица | Записей | Использование в коде | Статус | Рекомендация |
|---------|---------|----------------------|--------|--------------|
| `survey_assignments` | 0 | ❌ Нет | **Удалить** | Устаревшая таблица, функционал перенесен в `survey_360_assignments` |
| `user_achievements` | 0 | ❌ Нет | **Удалить** | Функционал достижений не реализован |
| `products` | N/A | ❌ Ссылка в коде, но таблицы не существует | **Очистить код** | Удалить упоминания из `tableConfig.ts` |
| `user_trade_points` | N/A | ❌ Ссылка в коде, но таблицы не существует | **Очистить код** | Удалить упоминания из `tableConfig.ts` |

### Частично используемые таблицы (есть в коде, но минимальное использование)

| Таблица | Записей | Использование в коде | Статус | Рекомендация |
|---------|---------|----------------------|--------|--------------|
| `manufacturers` | 5 | ⚠️ Только в справочниках | **Оставить** | Используется для управления производителями товаров |
| `trade_points` | 6 | ✅ Используется в MainContent | **Оставить** | Активно используется для торговых точек |
| `certifications` | N/A | ✅ Используется в GradesManager | **Оставить** | Связь с грейдами (сертификаты для повышения) |
| `competency_levels` | N/A | ✅ Используется в useDevelopmentTasks | **Оставить** | Используется для генерации задач развития |
| `departments` | N/A | ✅ Активно используется | **Оставить** | Широко используется по всей системе |
| `track_types` | 3 | ⚠️ Только справочник | **Оставить** | Используется для классификации карьерных треков |

### Используемые таблицы с триггерами на обновление

| Таблица | Записей | Использование | Описание |
|---------|---------|---------------|----------|
| `user_skills` | 5 | ✅ Триггер + хук | Обновляется автоматически из `hard_skill_results` |
| `user_qualities` | 13 | ✅ Триггер + хук | Обновляется автоматически из `soft_skill_results` |
| `user_career_progress` | 1 | ✅ Активно | Отслеживание прогресса по карьерным трекам |

---

## 🔍 Детальный анализ по таблицам

### 1. ❌ `survey_assignments` - **УДАЛИТЬ**

**Проблема:**
- Таблица существует в БД, но **не используется** в коде
- 0 записей в базе
- Устаревший функционал

**Обоснование для удаления:**
- Функционал опросов полностью перенесен в `survey_360_assignments`
- В текущей архитектуре используется модель диагностических этапов с автоматическим созданием назначений
- Никакие компоненты не обращаются к этой таблице

**Где упоминается:**
- Только в `src/integrations/supabase/types.ts` (автогенерация)

**Действие:**
```sql
-- Миграция для удаления таблицы
DROP TABLE IF EXISTS public.survey_assignments CASCADE;
```

---

### 2. ❌ `user_achievements` - **УДАЛИТЬ**

**Проблема:**
- Таблица существует, но функционал достижений **не реализован**
- 0 записей в базе
- Нет UI компонентов для работы с достижениями

**Обоснование для удаления:**
- Нет таблицы `achievements` (родительская таблица)
- Нет ни одного хука или компонента для работы с достижениями
- В роадмапе нет планов по реализации этого функционала

**Где упоминается:**
- Только в `src/integrations/supabase/types.ts` (автогенерация)

**Действие:**
```sql
-- Миграция для удаления таблицы
DROP TABLE IF EXISTS public.user_achievements CASCADE;
```

---

### 3. ❌ `products` - **ТАБЛИЦА НЕ СУЩЕСТВУЕТ, ОЧИСТИТЬ КОД**

**Проблема:**
- Таблица **не существует** в БД
- Но есть ссылки в коде: `src/components/admin/tableConfig.ts`, `src/pages/admin/ReferenceTablePage.tsx`

**Где упоминается:**
- `src/components/admin/tableConfig.ts`: `products: { manufacturer_id: { table: 'manufacturers', display: 'name' } }`
- `src/pages/admin/ReferenceTablePage.tsx`: `'products': { title: 'Товары', ... }`
- `src/pages/admin/DashboardPage.tsx`: админ-меню

**Действие:**
- Удалить все ссылки на `products` из кода
- Если функционал товаров нужен — создать таблицу и реализовать функционал
- Если нет — очистить код

---

### 4. ❌ `user_trade_points` - **ТАБЛИЦА НЕ СУЩЕСТВУЕТ, ОЧИСТИТЬ КОД**

**Проблема:**
- Таблица **не существует** в БД
- Но есть ссылки в коде

**Где упоминается:**
- `src/components/admin/tableConfig.ts`
- `src/pages/AdminPage.tsx`
- `src/hooks/useUserTradePoints.ts` - **весь хук бесполезен**

**Действие:**
- Удалить хук `src/hooks/useUserTradePoints.ts`
- Удалить все ссылки на `user_trade_points` из конфигураций

---

### 5. ⚠️ `manufacturers` - **ОСТАВИТЬ** (минимальное использование)

**Использование:**
- ✅ Справочник в админ-панели
- ✅ Используется в `ReferenceTableView.tsx`
- ✅ 5 записей в БД

**Статус:** Оставить, так как это часть справочной системы

---

### 6. ✅ `certifications` - **ОСТАВИТЬ** (активно используется)

**Использование:**
- ✅ Используется в `GradesManager.tsx` (связь грейдов с сертификатами)
- ✅ Поле `certification_id` в таблице `grades`
- ✅ Справочник в админ-панели

**Статус:** Активно используется в модуле грейдов

---

### 7. ✅ `competency_levels` - **ОСТАВИТЬ** (критически важная)

**Использование:**
- ✅ Используется в `useDevelopmentTasks.ts` для генерации задач развития
- ✅ Связь с `development_tasks` через `competency_level_id`
- ✅ Справочник уровней владения компетенциями (1-5)

**Статус:** Критически важная для модуля развития

---

### 8. ✅ `departments` - **ОСТАВИТЬ** (широко используется)

**Использование:**
- ✅ 76 упоминаний в 21 файле
- ✅ Используется в профилях, аналитике, управлении пользователями
- ✅ Критически важная справочная таблица

**Статус:** Активно используется по всей системе

---

### 9. ✅ `trade_points` - **ОСТАВИТЬ** (активно используется)

**Использование:**
- ✅ Используется в `MainContent.tsx` (отображение текущей торговой точки)
- ✅ Используется в `useUserTradePoints.ts`
- ✅ Отображение карты с координатами
- ✅ 6 записей в БД

**Статус:** Активно используется для функционала торговых точек

---

### 10. ⚠️ `track_types` - **ОСТАВИТЬ** (справочная)

**Использование:**
- ✅ Используется как `track_type_id` в `career_tracks`
- ✅ Справочник типов карьерных треков
- ✅ 3 записи в БД

**Статус:** Оставить, часть модуля карьерных треков

---

### 11. ✅ `user_skills` - **ОСТАВИТЬ** (триггер + активное использование)

**Использование:**
- ✅ Обновляется автоматически триггером `update_user_skills_from_survey()`
- ✅ Используется в `useUserSkills.ts`
- ✅ 5 записей в БД

**Статус:** Критически важная, обновляется автоматически

---

### 12. ✅ `user_qualities` - **ОСТАВИТЬ** (триггер + активное использование)

**Использование:**
- ✅ Обновляется автоматически триггером `update_user_qualities_from_survey()`
- ✅ Используется в `useUserQualities.ts`, `useCompetencyProfile.ts`
- ✅ 13 записей в БД

**Статус:** Критически важная, обновляется автоматически

---

### 13. ✅ `user_career_progress` - **ОСТАВИТЬ** (активно используется)

**Использование:**
- ✅ Используется в `useUserCareerProgress.ts`, `useCareerTracks.ts`
- ✅ Отслеживание прогресса пользователя по карьерным трекам
- ✅ 1 запись в БД

**Статус:** Критически важная для модуля карьерного развития

---

## 🗑️ Неиспользуемые поля в таблицах

### Таблица `users`

Потенциально неиспользуемые поля (требует дополнительной проверки):
- `phone` - есть ли UI для отображения?
- `work_address` - используется?
- `store_number` - связь с торговыми точками?

### Таблица `tasks`

Поля с минимальным использованием:
- `kpi_expected_level` - используется только в типах
- `kpi_result_level` - используется только в типах
- `competency_ref` - используется только в типах

---

## 📋 Рекомендации по очистке

### Немедленные действия (высокий приоритет)

1. **Удалить таблицу `survey_assignments`**
   ```sql
   DROP TABLE IF EXISTS public.survey_assignments CASCADE;
   ```

2. **Удалить таблицу `user_achievements`**
   ```sql
   DROP TABLE IF EXISTS public.user_achievements CASCADE;
   ```

3. **Удалить хук `src/hooks/useUserTradePoints.ts`** (ссылается на несуществующую таблицу)

4. **Очистить ссылки на несуществующие таблицы:**
   - Удалить `products` из `tableConfig.ts`, `ReferenceTablePage.tsx`, `DashboardPage.tsx`
   - Удалить `user_trade_points` из `tableConfig.ts`, `AdminPage.tsx`

### Средний приоритет

5. **Провести аудит полей таблицы `users`:**
   - Определить, какие поля реально используются
   - Документировать назначение каждого поля

6. **Провести аудит полей таблицы `tasks`:**
   - Определить, используются ли `kpi_expected_level`, `kpi_result_level`, `competency_ref`
   - Если нет — рассмотреть удаление или документировать для будущего использования

### Низкий приоритет

7. **Оптимизация индексов:**
   - После удаления таблиц проверить и удалить неиспользуемые индексы
   - Добавить индексы для часто запрашиваемых полей (если их нет)

---

## ✅ Итоговые цифры

- **Таблиц к удалению:** 2 (`survey_assignments`, `user_achievements`)
- **Ссылок на несуществующие таблицы в коде:** 2 (`products`, `user_trade_points`)
- **Хуков к удалению:** 1 (`useUserTradePoints.ts`)
- **Таблиц к сохранению:** 11 (все остальные активно используются)

---

## 🔄 Следующие шаги

1. ✅ Создать миграцию для удаления неиспользуемых таблиц
2. ⏳ Очистить код от ссылок на несуществующие таблицы
3. ⏳ Провести аудит полей в таблицах `users` и `tasks`
4. ⏳ Обновить документацию по структуре БД

---

**Примечание:** Все рекомендации основаны на статическом анализе кода и запросах к БД. Перед удалением критически важно провести тестирование всех модулей системы.
